<h1> 🎭 <%= @artist.name %> 🎭 </h1>

<a href="/artists">
  👈 Back to all artists
</a>

<h2>Songs:</h2>
<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="/songs/<%=song.slug%>">
        <%=song.name%>
      </a>
    </li>
  <% end %>
</ul>

<h2>Genres:</h2>
<ul>
  <% @genres.each do |genre| %>
    <li>
      <a href="/genres/<%=genre.slug%>">
        <%=genre.name%>
      </a>
    </li>
  <% end %>
</ul>
